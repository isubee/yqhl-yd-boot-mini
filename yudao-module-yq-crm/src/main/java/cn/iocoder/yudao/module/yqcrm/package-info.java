/**
 * yqcrm 包下，放 YQ CRM 的功能。
 *
 * 1. Controller URL：以 /yq-crm/ 开头，避免和其它 Module 冲突
 * 2. DataObject 表名：以 yq_crm_ 开头，方便在数据库中区分
 *
 * 注意，YQ CRM 模块下的类名前缀使用 YqCrm。
 */
package cn.iocoder.yudao.module.yqcrm;
